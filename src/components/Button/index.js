import React from 'react';
import styled from 'styled-components';
import arrow from './icons/arrow-back.svg';
const StyledButton = styled.div`
    
`

export default({icon, onClick, text}) => (
    <StyledButton>text</StyledButton>
)
